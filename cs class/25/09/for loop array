#include<stdio.h>

int main() {
  // printing the array elements 
int a[3]= {12, 7, 8, 9, 4};

  for (int i = 0; i<=2; i++)
  printf("%d\n", a[i]);

  // i is called as the index variable

  return 0;
}
